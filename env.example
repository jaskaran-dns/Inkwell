# --- Supabase (safe to expose in the browser: anon key is public by design) ---
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=

# --- Server-only secrets (NEVER prefix with NEXT_PUBLIC_, never import in client components) ---
SUPABASE_SERVICE_ROLE_KEY=

# --- App config ---
NEXT_PUBLIC_SITE_URL=http://localhost:3000
